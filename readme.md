# Dotfiles

This is a global repository for most of my config files 

### Neovim
Note that the neovim config in ```nvim/.config/nvim``` is a submodule. 
So when cloning the repository, you should do it with ```git clone
--recurse-submodules https://github.com/bsssssss/dotfiles.git```
